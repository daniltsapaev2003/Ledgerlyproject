// Здесь ранее экспортировались функции интеграции с Replit (setupAuth, isAuthenticated, getSession).
// Эти функции обычно используются для:
// 1. Аутентификации пользователей через платформу Replit (например, если проект развернут непосредственно на Replit или ожидается, что пользователи будут логиниться через Replit).
// 2. Хранения/проверки сессий и управления процессом авторизации через Replit.
//
// Зачем они нужны: 
// - Эти интеграции актуальны, если ваше приложение запускается на платформе Replit и вы хотите использовать встроенное управление пользователями Replit или ограничивать доступ к API для аккаунтов Replit.
// - Если проект не запускается на Replit, эти модули не нужны.
//
// Как убрать Replit интеграции:
// Варианты:
// 1. Просто удалить этот (и аналогичные) экспорт — если вы не используете Replit авторизацию.
// 2. Удалить/отключить весь каталог `replit_integrations` и любые импорты из него по всему проекту, чтобы код не тянул лишние зависимости.
// 3. Проверить, не используются ли эти функции (setupAuth, isAuthenticated, getSession) в других файлах, и убрать/заменить их на свою авторизацию.
//
// Безопасно — если вы не используете Replit как login-провайдер, эти экспорты можно закомментировать или удалить:
// Из этого файла экспортируются:

// - authStorage и его тип IAuthStorage из модуля "./storage" (интерфейс и реализация для хранения пользовательских сессий/токенов)
// - функция registerAuthRoutes из "./routes" (регистрирует эндпоинты для авторизации через Replit OIDC)

export { authStorage, type IAuthStorage } from "./storage";
export { registerAuthRoutes } from "./routes";
